// add solution here
function theBeatlesPlay(arrayMusician, arrayInstrument) {
  var emptyArray = [];
  for ( i = 0; i < arrayMusician.length; i++) {
    emptyArray = array.push(arrayMusician[i] + " plays " + arrayInstrument[i]);
  }
  return emptyArray;
}